# UI
streamlit>=1.30,<2.0

# Audio processing (requires ffmpeg installed separately on the system, e.g. via "ffmpeg-python>=0.2")
pydub>=0.25.1

# ASR + model runtime
torch>=2.1,<3.0
transformers>=4.40,<5.0

# Hugging Face model download helpers (often pulled in by transformers, but good to be explicit)
huggingface-hub>=0.20
accelerate>=0.26
safetensors>=0.4
